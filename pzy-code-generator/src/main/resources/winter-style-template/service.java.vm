package ${package.Service};

import ${package.Entity}.${entity};
import org.pzy.opensource.mybatisplus.service.ServiceTemplate;

/**
 * $!{table.name} 表相关$!{table.comment} 服务接口
 *
 * @author ${author}
 * @since ${date}
 */
public interface ${table.serviceName} extends ServiceTemplate<${entity}> {

}
